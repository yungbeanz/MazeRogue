$(document).ready(function(){
	// Game launch functions.
	RogueUtil.launchTitle(); // Launches MazeRogue.
	RogueUtil.userInput(); // Initializes userInput for playing. Should only be called once, in this location.
});